---
name: hermes-cortex
description: "Hermes 大脑系统 — 基于 Karpathy LLM Wiki 模式 + 知识图谱 + 语义检索的 Agent 记忆与知识管理系统"
trigger: "当需要管理知识、检索记忆、整理笔记、构建关联图谱、或进行知识驱动的决策时使用"
version: "2.0.0"
created: 2026-06-13
updated: 2026-06-15
tags: [knowledge-management, memory, graph, obsidian, RAG, second-brain]
---

# Hermes 大脑系统

> 基于 Karpathy LLM Wiki 模式 + 知识图谱 + 语义检索的 Agent 记忆与知识管理系统

## 核心理念

**Karpathy LLM Wiki 模式**：别每次都去翻原始资料，而是让 LLM 一点点维护一个长期的 wiki。知识像复利一样累积。

**核心设计原则**：🧠 **"大脑"必须是自动的**。一个需要手动触发的"大脑"不是大脑，只是一堆脚本。所有知识管理操作（检索、学习、更新、维护）必须在后台自动运行，用户无感知。

**三大支柱**：
1. **记忆层** — 短期（热缓存）+ 中期（会话记忆）+ 长期（持久化知识）
2. **知识图谱** — 实体 + 概念 + 关联关系的网络
3. **检索层** — 语义搜索 + BM25 + 关联图遍历

**自动化三原则**：
1. **提问自动检索** — 用户问问题时，先检索知识库再回答
2. **学习自动记录** — 学到新知识时，自动创建笔记并更新索引
3. **对话结束自动维护** — 每次对话结束，自动更新热缓存和检查孤立笔记

---

## 架构设计

```
输入层（用户对话、外部资料、工具结果、系统事件）
    ↓
处理层（信息抽取、知识融合、图谱构建、索引更新）
    ↓
存储层（热缓存、索引、图谱、日志、笔记库）
    ↓
检索层（语义搜索、关键词搜索、图谱遍历、元数据过滤）
    ↓
输出层（回答、建议、洞察、行动）
```

---

## 笔记结构规范

### 实体笔记

描述具体的人、工具、项目、组织。

```markdown
---
title: [实体名称]
type: entity
created: YYYY-MM-DD
tags: [entity, 类别]
关联: [[相关笔记1]] | [[相关笔记2]]
---

# [实体名称]

## 概述
简要描述这个实体是什么。

## 属性
- **类型**: 人/工具/项目/组织
- **状态**: 活跃/归档/废弃

## 关联
- [[相关实体1]] — 关系描述
```

### 概念笔记

描述抽象的想法、方法论、设计模式。

```markdown
---
title: [概念名称]
type: concept
created: YYYY-MM-DD
tags: [concept, 领域]
关联: [[相关笔记1]] | [[相关笔记2]]
---

# [概念名称]

## 定义
清晰定义这个概念。

## 核心原则
1. 原则1
2. 原则2

## 相关概念
- [[相关概念1]] — 关系描述
```

### 探索笔记

描述研究过程、发现、分析。

```markdown
---
title: [探索主题]
type: exploration
created: YYYY-MM-DD
tags: [exploration, 主题]
关联: [[相关笔记1]] | [[相关笔记2]]
---

# [探索主题]

## 背景
为什么进行这次探索。

## 发现
详细描述。

## 行动项
- [ ] 行动1
- [ ] 行动2
```

### 日记笔记

记录日常任务、状态、思考。

```markdown
---
title: YYYY-MM-DD
type: daily
created: YYYY-MM-DD
tags: [daily]
---

# YYYY-MM-DD

## 任务
- [x] 完成的任务
- [ ] 未完成的任务
```

---

## 脚本工具

所有脚本位于 `D:\Hermes\skills\hermes-cortex\scripts\`

### 1. 热缓存 (`hot_cache.py`)

自动扫描最近修改的笔记，更新 `index.md` 顶部的热缓存区域。

```bash
python scripts/hot_cache.py
```

### 2. 语义索引 (`semantic_index.py`)

用 `all-MiniLM-L6-v2` 模型生成笔记的向量嵌入，存储到 SQLite，支持语义相似度搜索。

```bash
python scripts/semantic_index.py index    # 构建索引
python scripts/semantic_index.py search "查询内容"  # 语义搜索
python scripts/semantic_index.py stats    # 显示统计
```

### 3. 自动研究 (`auto_research.py`)

分析现有知识，发现空白和薄弱点，生成研究建议。

```bash
python scripts/auto_research.py discover  # 发现知识空白
python scripts/auto_research.py report    # 自进化报告
python scripts/auto_research.py suggest   # 研究建议
```

### 4. 知识图谱 (`build_graph.py`)

从 Obsidian Vault 中抽取实体和关系，构建知识图谱。

```bash
python scripts/build_graph.py
```

### 5. 知识检索 (`retrieve.py`)

三层检索（热缓存、索引、图谱），返回相关笔记。

```bash
python scripts/retrieve.py "查询内容"
```

### 6. 知识维护 (`maintain.py`)

孤立检测、关联推荐、过期清理、统计报告、引用验证。

```bash
python scripts/maintain.py validate   # 验证引用
python scripts/maintain.py isolated   # 找出孤立笔记
python scripts/maintain.py stats      # 生成统计
```

### 7. 自进化引擎 (`evolve.py`)

完整的自进化循环：发现空白 → 搜索补充 → 创建笔记 → 更新索引 → 更新热缓存。

```bash
python scripts/evolve.py run      # 运行一次自进化循环
python scripts/evolve.py dry-run  # 干运行（不创建笔记）
python scripts/evolve.py status   # 显示自进化状态
```

### 8. Cron 任务 (`cron.py`)

每天自动运行自进化循环。

```bash
python scripts/cron.py setup    # 设置 cron 任务
python scripts/cron.py remove   # 移除 cron 任务
python scripts/cron.py status   # 查看 cron 状态
python scripts/cron.py run      # 手动运行一次
```

### 9. 自动化 Hook (`brain_hook.py`)

**这是让 Hermes Brain 成为真正"大脑"的关键脚本。**

在每次对话结束时自动运行，更新热缓存和索引。

```bash
python scripts/brain_hook.py
```

**自动化流程**：
1. 更新热缓存（hot_cache.py）
2. 检查语义索引是否过期（>24小时则更新）
3. 检查孤立笔记（maintain.py）
4. 每 10 次对话自动检查优化（auto_optimize.py）

**关键设计**：用户无感知，后台自动运行。

### 10. 可观测性 (`observability.py`)

分析 Hermes 日志，追踪成本和性能。

```bash
python scripts/observability.py  # 生成可观测性报告
```

**报告内容**：
- Session 日志分析（token 使用、成本）
- 错误日志分析（错误类型统计）
- 工具使用分析（最常用工具 Top 10）

### 11. 自动优化 (`auto_optimize.py`)

**当知识库接近性能瓶颈时自动触发优化。**

```bash
python scripts/auto_optimize.py
```

**警戒线**：

| 指标 | 阈值 | 触发优化 |
|------|------|----------|
| 笔记数 | 300 | 归档 90 天未修改的笔记 |
| 索引大小 | 30MB | 增量索引优化 |
| 孤立笔记 | 20 | 清理孤立笔记 |
| 无效链接 | 50 | 清理无效链接 |
| 热缓存年龄 | 24h | 更新热缓存 |

**触发方式**：`brain_hook.py` 每 10 次对话自动调用一次。

---

## 自进化循环

```
discover（发现空白）
    ↓
suggest（生成建议）
    ↓
web_search（搜索补充）
    ↓
LLM 抽取（实体和概念）
    ↓
创建笔记（write_file）
    ↓
更新索引（semantic_index.py）
    ↓
更新热缓存（hot_cache.py）
    ↓
回到 discover
```

---

## 工具集成

### Obsidian 集成

笔记存储位置：`D:\ObsidianVault\`

```
D:\ObsidianVault\
├── index.md              # 主索引
├── SCHEMA.md             # 结构规范
├── log.md                # 操作日志
├── concepts/             # 概念笔记
├── entities/             # 实体笔记
├── raw/                  # 原始笔记
│   ├── exploration/      # 探索笔记
│   ├── research/         # 研究笔记
│   └── heartbeat/        # 心跳笔记
├── daily/                # 日记
└── hermes/               # Hermes 相关
```

### 记忆工具集成

```bash
memory(action='add', target='memory', content='...')
memory(action='replace', target='memory', old_text='...', new_text='...')
memory(action='remove', target='memory', old_text='...')
```

### 检索工具集成

```bash
search_files(pattern='...', target='content', path='D:\\ObsidianVault')
read_file(path='D:\\ObsidianVault\\index.md')
write_file(path='D:\\ObsidianVault\\new-note.md', content='...')
```

### Phoenix 集成

Phoenix（不死鸟）桌面控制工具可以用于：
- **桌面截图** — 捕获桌面状态
- **文件操作** — 读写文件
- **代码执行** — 运行 Python/shell
- **浏览器控制** — 执行 JS 和扫描网页

---

## 最佳实践

### 笔记命名规范

- **实体笔记**: `tool-name.md`, `person-name.md`, `project-name.md`
- **概念笔记**: `concept-name.md`, `methodology-name.md`
- **探索笔记**: `YYYY-MM-DD-exploration-topic.md`
- **日记**: `YYYY-MM-DD.md`

### 标签使用规范

- 每个笔记最多 3 个标签
- 标签按用途分类，不是按主题
- 新标签必须在 SCHEMA.md 中注册

### 关联维护规范

- 每个笔记至少有 1 个关联
- 关联关系要有描述（不只是链接）
- 定期检查关联的有效性

### 热缓存更新规范

- 每次会话后更新热缓存
- 热缓存只保留最近 500 字
- 热缓存是最近上下文的摘要

---

## ⚠️ 关键 Pitfall

### 0. GitHub 同步规则

**不要主动同步 GitHub。** 用户明确说："以后不用同步github，哪有天天更新的，以后我让你更新再更新"。

- ❌ 每次改完文件就上传 GitHub
- ❌ 自动同步、定期同步
- ✅ 用户说"更新 GitHub"时才同步
- ✅ 用户说"同步"时才同步

### 1. 一口气做完，不留"下一步"

用户明确拒绝任何形式的"待实现"、"下一步"、"计划中"。必须一次性完成所有功能，不留 TODO。
- ❌ "下一步建议：可以添加 X 功能"
- ❌ "待实现：Y 功能"
- ❌ "计划中：Z 优化"
- ❌ "还有建议或者下一步打算" — 用户说这句话时，意思是"把能做的都做完"

## 关键陷阱

### 0. 彻底融合 Hermes（非外挂模式）

Hermes Brain 不是"外挂脚本"，而是 Hermes 的"大脑"。必须通过以下机制融合：

**Shell Hooks**（自动生命周期）：
```yaml
# 在 AppData/Local/hermes/config.yaml 的 hooks: 下配置
hooks:
  on_session_start:
    - command: "python D:/Hermes/skills/hermes-cortex/scripts/hook_session_start.py"
      timeout: 30
  on_session_end:
    - command: "python D:/Hermes/skills/hermes-cortex/scripts/hook_session_end.py"
      timeout: 60
hooks_auto_accept: true
```

**allowlist**（首次安全确认）：
```json
// AppData/Local/hermes/shell-hooks-allowlist.json
{"approvals": [{"event":"on_session_start","command":"python D:/Hermes/skills/hermes-cortex/scripts/hook_session_start.py","approved_at":"..."}]}
```

**SOUL.md**（注入到 system prompt）：
在 `~/.hermes/SOUL.md` 中添加"大脑系统"章节，让 Agent 知道它有 brain 并主动使用。

**验证**：`hermes hooks list` 应显示 ✓ allowed，`hermes hooks doctor` 应显示 All healthy。

详见 `references/hermes-hooks-integration.md`。

### 1. 不要用 C 盘路径（但 Hermes 配置在 AppData）

用户明确拒绝 C 盘存储。所有文件必须放在 D 盘：
- **Skill 目录**: `D:\Hermes\skills\hermes-cortex\`
- **笔记库**: `D:\ObsidianVault\`
- **项目目录**: `D:\Hermes\`

**⚠️ 例外：Hermes 自身配置在 AppData**
- `config.yaml`: `C:\Users\20716\AppData\Local\hermes\config.yaml`（不是 `~/.hermes/config.yaml`）
- `shell-hooks-allowlist.json`: `C:\Users\20716\AppData\Local\hermes\shell-hooks-allowlist.json`
- **两个 config.yaml 共存**：`~/.hermes/config.yaml` 和 `AppData/Local/hermes/config.yaml`。Hermes 实际读的是 AppData 那个。
- 修改配置用 `hermes config set` 或 Python `yaml.dump` 直接写 AppData 文件。`hermes config set` 对复杂 YAML 会存为 JSON 字符串——必须用 Python yaml.dump。

### 2. Python 3.12 vs 3.13 环境

语义索引需要 Python 3.12 环境，因为 `sentence-transformers` 装在 Python 3.12 中：

```bash
# 语义索引用 Python 3.12
/c/Users/20716/AppData/Local/Programs/Python/Python312/python.exe scripts/semantic_index.py index

# 其他脚本用默认 python
python scripts/hot_cache.py
python scripts/auto_research.py
```

### 3. f-string 中不要有真实换行

Python f-string 不能有真实换行，必须用 `\n`：
```python
# ❌ 错误
print(f"
{text}")

# ✅ 正确
print(f"\n{text}")
```

### 4. GitHub README 质量要求

上传 GitHub 前必须参考热门项目的 README 格式（headroom 20KB、claude-obsidian 37KB、swarmvault 48KB）。README 必须像"说明书"，包含：
- 环境要求表格
- 分平台安装指南
- 故障排除
- FAQ
- 竞品对比
- 路线图

详见 `references/github-readme-pattern.md`。

### 4. Shell Hooks 配置陷阱

**config.yaml 格式错误**：`hermes config set hooks '{"on_session_start": [...]}'` 会把 hooks 存为 JSON 字符串（string），而不是 YAML dict。`_parse_hooks_block()` 检查 `isinstance(hooks_cfg, dict)` 会失败，返回空列表。

**Fix**：用 Python yaml.dump 直接写入 AppData config.yaml。

### 5. Windows bash 编码问题

Git Bash (MSYS2) 在 Windows 上输出中文会编码错误。Hook 脚本必须用 Python wrapper 而不是 bash。

### 6. MCP 上传截断

`mcp_github_create_or_update_file` 会截断大文件（25KB → 5KB）。必须用 Python + curl + GitHub PAT。详见 `references/github-upload-workflow.md`。

### 7. 自动优化系统

当知识库接近性能瓶颈时自动触发优化：

| 指标 | 警戒线 | 优化动作 |
|------|--------|----------|
| 笔记数 | 300 | 归档过期笔记 |
| 索引大小 | 30MB | 增量索引优化 |
| 孤立笔记 | 20 | 清理孤立笔记 |
| 无效链接 | 50 | 清理无效链接 |
| 热缓存年龄 | 24h | 更新热缓存 |

详见 `scripts/auto_optimize.py`。

## References

- `references/karpathy-llm-wiki.md` — Karpathy LLM Wiki 模式详解
- `references/hermes-hooks-integration.md` — Shell Hooks 集成指南
- `references/harness-engineering.md` — Harness Engineering 研究（2026-06-14）
- `references/harness-engineering-comprehensive.md` — Harness Engineering 综合研究（2026-06-15，含 8 个核心项目、5 个子系统、6 步治理管道、三层可观测性）
- `references/github-upload-workflow.md` — GitHub 上传工作流（解决 MCP 截断问题）
- `references/github-readme-pattern.md` — GitHub 专业 README 格式参考

**正确做法**：用 Python yaml.dump 直接写入 config.yaml：
```python
import yaml
from pathlib import Path
config_path = Path('C:/Users/20716/AppData/Local/hermes/config.yaml')
with open(config_path, 'r', encoding='utf-8') as f:
    config = yaml.safe_load(f) or {}
config['hooks'] = {
    'on_session_start': [{'command': 'python D:/Hermes/skills/hermes-cortex/scripts/hook_session_start.py', 'timeout': 30}],
    'on_session_end': [{'command': 'python D:/Hermes/skills/hermes-cortex/scripts/hook_session_end.py', 'timeout': 60}]
}
with open(config_path, 'w', encoding='utf-8') as f:
    yaml.dump(config, f, allow_unicode=True, default_flow_style=False, sort_keys=False)
```

**command 字段格式**：Shell hooks 只读 `command` 字段（单个字符串），用 `shlex.split()` 解析。**不要用 `command` + `args` 分开写**，直接写完整命令。

**Allowlist 路径**：`C:\Users\20716\AppData\Local\hermes\shell-hooks-allowlist.json`（不是 `~/.hermes/`）。

**Windows 编码问题**：Git Bash (MSYS2) 输出中文会触发 UnicodeDecodeError。**必须用 Python wrapper 脚本**，不要直接用 bash 脚本作为 hook command。

### 5. gh CLI punycode 报错 + bulk upload 替代方案

`gh api` 命令在 Node.js 22.x 上报 `TypeError: Cannot read properties of undefined (reading 'options')`，是 punycode 模块弃用导致的。

**替代方案（按优先级）**：

1. **`mcp_github_create_or_update_file`** — 最简单，单个文件直接传，但批量上传需要逐个调用
2. **curl + API token** — 从 MCP server config 提取 token，批量上传：

```python
import yaml, json, base64, subprocess

# 从 MCP config 提取 token
with open("C:/Users/20716/AppData/Local/hermes/config.yaml") as f:
    cfg = yaml.safe_load(f)
token = cfg["mcp_servers"]["github"]["env"]["GITHUB_PERSONAL_ACCESS_TOKEN"]

# 批量上传
for filepath in files:
    with open(filepath, "r", encoding="utf-8") as f:
        content = f.read()
    b64 = base64.b64encode(content.encode()).decode()
    payload = json.dumps({"message": f"feat: add {filepath}", "content": b64, "branch": "main"})
    subprocess.run([
        "curl", "-s", "-X", "PUT",
        f"https://api.github.com/repos/{owner}/{repo}/contents/{filepath}",
        "-H", f"Authorization: token {token}",
        "-H", "Content-Type: application/json",
        "-d", payload
    ], capture_output=True, text=True)
```

**⚠️ 注意**：`GITHUB_PERSONAL_ACCESS_TOKEN` 在 `mcp_servers.github.env` 下，不是顶层 key。

### 6. 一口气做完，不要分步确认

用户偏好批量处理，不要问"继续吗"、"下一步"、"要我做吗"。一次性完成所有操作，不留"待实现"或"下一步"。

### 6. 不要创建 Web UI / Dashboard

用户明确拒绝 Web UI 仪表板，不要创建 dashboard.py 或类似的可视化工具。

### 7. GitHub 不主动同步

用户明确要求：不要每次改完文件就上传 GitHub。等用户说"更新 GitHub"再同步。天天更新没意义。

### 8. Skill 自检流程

打包前必须自检：
- 用 `grep "^##"` 检查 SKILL.md 是否有重复章节
- 删除冗余文件（COMPLETION_REPORT.md、README.md 在 skill 目录中不需要）
- `rm -rf scripts/__pycache__` 清理缓存
- 确认所有路径指向 D 盘
- `python -m py_compile scripts/*.py` 语法检查

### 7. GitHub README 格式要求

用户对 README 质量要求很高。参考 headroom (20KB) 和 claude-obsidian (37KB) 的格式：
- ASCII art logo 开头
- 一句话介绍 + 快速导航链接
- 数据说话（具体数字、统计）
- 用 `<details>` 折叠详细内容
- 竞品对比表格
- "When to use · When to skip" 章节
- Requirements 表格（最低/推荐配置）
- 不要写废话，信息密度要高

详见 `references/github-readme-pattern.md`。

### 8. GitHub 上传工作流

当 `git push` 因网络问题（代理、防火墙）失败时的替代方案。有三种方式，按优先级：

**方式 1：`mcp_github_create_or_update_file`** — 最简单，单个文件直接传。

**方式 2：Python + curl + API token** — 批量上传或 MCP 工具截断内容时使用。

**方式 3：`gh api`** — 如果 gh CLI 没有 punycode 报错的话。

#### ⚠️ 关键陷阱 1：MCP 工具会截断大文件内容

`mcp_github_create_or_update_file` 上传大文件（>5KB）时，**内容会被截断**。实测：
- SKILL.md: 24,881 bytes → 4,564 bytes（截断 82%）
- README.md: 20,011 bytes → 5,872 bytes（截断 71%）

**症状**：上传成功但 GitHub 上文件大小明显小于本地。

**解决方案**：用 Python + curl 直接调用 GitHub API：

```python
import yaml, json, base64, subprocess

# 从 MCP config 提取 token
with open("C:/Users/20716/AppData/Local/hermes/config.yaml") as f:
    cfg = yaml.safe_load(f)
token = cfg["mcp_servers"]["github"]["env"]["GITHUB_PERSONAL_ACCESS_TOKEN"]

# 上传函数（支持创建和更新）
def upload_file(filepath, content, token, sha=None):
    b64 = base64.b64encode(content.encode()).decode()
    payload = {"message": f"feat: add {filepath}", "content": b64, "branch": "main"}
    if sha:
        payload["sha"] = sha
    result = subprocess.run([
        "curl", "-s", "-X", "PUT",
        f"https://api.github.com/repos/{owner}/{repo}/contents/{filepath}",
        "-H", f"Authorization: token {token}",
        "-H", "Content-Type: application/json",
        "-d", json.dumps(payload)
    ], capture_output=True, text=True)
    return '"sha"' in result.stdout

# 批量上传
for filepath in files:
    with open(filepath, "r", encoding="utf-8") as f:
        content = f.read()
    upload_file(filepath, content, token)
```

**⚠️ `GITHUB_PERSONAL_ACCESS_TOKEN` 在 `mcp_servers.github.env` 下，不是顶层 key。**

#### ⚠️ 关键陷阱 2：更新已有文件必须传 sha 参数

GitHub API 的 `PUT /contents/{path}` 接口：
- **创建新文件**：不需要 `sha` 参数
- **更新已有文件**：**必须**传入当前文件的 `sha` 参数，否则返回 422

```python
# 获取 sha
def get_sha(filepath, token):
    result = subprocess.run([
        "curl", "-s",
        f"https://api.github.com/repos/{owner}/{repo}/contents/{filepath}",
        "-H", f"Authorization: token {token}"
    ], capture_output=True, text=True)
    return json.loads(result.stdout).get("sha")

# 更新文件
sha = get_sha("README.md", token)
upload_file("README.md", new_content, token, sha)
```

#### 工作流顺序（避免重复工作）

1. 先在本地写好完整内容
2. 验证内容质量（大小、章节、格式）
3. 一次性上传到 GitHub
4. **上传后验证文件大小是否匹配**（关键！）

```bash
# 验证上传结果
curl -s "https://api.github.com/repos/{owner}/{repo}/contents/{path}" | grep '"size"'
```

**不要**：先上传简化版 → 再更新完整版（浪费时间，容易出错）

详见 `references/github-upload-workflow.md`。

### 9. Skill 打包前自检清单

打包/上传前必须完成：

```bash
# 1. 检查重复章节
grep "^##" SKILL.md | sort | uniq -d

# 2. 语法检查
python -m py_compile scripts/*.py

# 3. 清理缓存
rm -rf scripts/__pycache__

# 4. 检查路径
grep -r "C:\\\\" . && echo "❌ 发现 C 盘路径" || echo "✅ 全部 D 盘"

# 5. 检查文件完整性
find . -type f | wc -l  # 确认文件数量

# 6. 删除冗余文件
rm -f COMPLETION_REPORT.md README.md  # skill 目录中不需要
```

---

## GitHub 热榜对比

详见 `references/github-comparison.md` — 包含 20+ 个项目的详细对比分析、功能矩阵、优先级路线图。

## GitHub README 格式

详见 `references/github-readme-format.md` — 参考 claude-obsidian (6.6k⭐) 的专业 README 格式。

---

## 系统体检流程

当用户要求"全面体检"或"检查系统状态"时，运行以下检查：

```bash
# 1. 文件结构
find . -type f | wc -l
du -sh .

# 2. 脚本语法检查
for f in scripts/*.py; do python -m py_compile "$f" 2>&1 && echo "✅ $f" || echo "❌ $f"; done

# 3. Harness 验证
python scripts/verify_harness.py

# 4. 知识库状态
python scripts/maintain.py stats
python scripts/auto_optimize.py

# 5. 热缓存、语义索引、知识图谱
python scripts/hot_cache.py
python scripts/semantic_index.py stats
python scripts/build_graph.py

# 6. Shell Hooks 和计划任务
cat "C:\Users\20716\AppData\Local\hermes\shell-hooks-allowlist.json"
powershell -Command "Get-ScheduledTask -TaskName 'Hermes Cortex Auto Evolve'"

# 7. 记忆系统
python -c "import yaml; config=yaml.safe_load(open('C:/Users/20716/AppData/Local/hermes/config.yaml')); print('memory_enabled:', config.get('memory', {}).get('memory_enabled', False))"
```

**健康度评分标准**：
- 95-100：优秀
- 80-94：良好
- 60-79：一般
- <60：需要关注

## Harness Engineering 集成

Hermes Cortex 已深度整合 Harness Engineering 的 5 个子系统：

| 子系统 | 作用 | 文件 |
|--------|------|------|
| **Instructions** | 告诉 Agent 做什么 | SKILL.md、SOUL.md |
| **State** | 跟踪进度 | progress.md、feature_list.json、热缓存、语义索引、知识图谱 |
| **Verification** | 验证完成 | auto_optimize.py、maintain.py、verify_harness.py |
| **Scope** | 控制范围 | feature_list.json 的 status 字段 |
| **Session Lifecycle** | 会话生命周期 | init.sh、Shell Hooks |

### Agent 会话生命周期

```
START → 读 progress.md + feature_list.json → 选择功能 → 实现 + 验证 → 更新进度 + 热缓存 + 知识图谱
```

### Harness 文件

| 文件 | 用途 |
|------|------|
| `progress.md` | 进度日志 |
| `feature_list.json` | 功能列表（机器可读） |
| `init.sh` | 会话初始化脚本 |
| `scripts/verify_harness.py` | Harness 验证脚本 |

### 验证命令

```bash
# 完整验证
python scripts/verify_harness.py

# 单项验证
python scripts/auto_optimize.py      # 警戒线检查
python scripts/maintain.py stats     # 统计报告
python scripts/hot_cache.py          # 热缓存更新
python scripts/build_graph.py        # 知识图谱构建
```

## SOUL.md 压缩技术

**问题**：SOUL.md 每条消息都会加载，11KB 太浪费（~2,948 tokens/轮）。

**解决方案**：压缩到 <1KB，只保留核心规则。

**压缩前（11,792 bytes）**：
- 详细人格描述（5KB）→ 删除
- 长篇示例（3KB）→ 删除
- 历史记录（2KB）→ 删除
- 核心规则（1KB）→ 保留

**压缩后（777 bytes）**：
```markdown
# SOUL.md

## 语气
- 直接、简洁、有态度
- 不废话、不客套、不附和
- 干燥幽默，偶尔冷吐槽

## 核心原则
1. 效率至上：先给结论，再给理由
2. 结果导向：方案必须可执行
3. 自动推进：多步骤任务一气呵成，不中途问"是否继续"
4. 诚实直接：不知道就说不知道

## 禁忌
- ❌ "好的，我来帮您分析一下~"
- ❌ "这是一个很好的问题！"
- ❌ 过度解释、虚假谦虚、假装热情

## 大脑系统
- 回答前先检索知识库（retrieve.py）
- 学到新知识自动创建笔记到 D:\ObsidianVault\
```

**效果**：每轮节省 ~2,754 tokens（93% 减少）。

**原则**：SOUL.md 是"系统级人格指令"，不是"详细操作手册"。详细操作规则放在 SKILL.md 中，按需加载。

---

## 技能描述优化

**问题**：技能描述太长或太短，影响 AI 理解和加载。

**解决方案**：用 `optimize_skills.py` 脚本自动优化。

```bash
python scripts/optimize_skills.py
```

**优化规则**：
- 英文→中文翻译（更清晰）
- 去掉冗余前缀（"Configure, extend, or contribute to" → "配置、扩展、贡献"）
- 突出核心功能
- 保持简洁（<50 字符）

**实际效果**（2026-06-15）：
- 优化了 7/44 个技能描述
- 修复了 5 个低质量描述（<25 字符）
- 每轮节省 ~222 tokens

**低质量描述修复**：
当审计工具检测到"low-quality description"时，需要手动补充描述细节：
```python
# 示例：修复 llm-wiki 的低质量描述
path = 'C:/Users/20716/.hermes/skills/research/llm-wiki/SKILL.md'
with open(path, 'r', encoding='utf-8') as f:
    content = f.read()
content = content.replace('description: "Karpathy 知识库."', 'description: "Karpathy LLM Wiki——构建/查询互链知识库，适合研究和学习"')
with open(path, 'w', encoding='utf-8') as f:
    f.write(content)
```

---

## 技能审计与清理

当技能数量过多时，使用 `hermes-skill-audit` 工具进行审计和清理。

**安装位置**：`D:\Hermes\skills\hermes-skill-audit\`

**使用方法**：
```bash
# 完整审计
python D:/Hermes/skills/hermes-skill-audit/scripts/audit.py

# 快速摘要
python D:/Hermes/skills/hermes-skill-audit/scripts/audit.py --summary

# 预览清理
python D:/Hermes/skills/hermes-skill-audit/scripts/audit.py --dry-run

# 执行清理
python D:/Hermes/skills/hermes-skill-audit/scripts/audit.py --fix
```

**⚠️ 清理时不要过度删除 Agent 自身在用的技能。** 用户会问"你平时用不到吗？"——plan、bug-hunter、systematic-debugging、hermes-agent-skill-authoring、squirrel 等是 Agent 日常工作流的一部分，必须保留。

**清理标准**：
- **删**：用户场景不匹配（Windows 用户删 Apple/macOS、不搞音乐删 media、不用 Docker 删 s6）
- **删**：从没用过且无明确场景的（gaming、gifs、libreoffice 等 Antigravity 装的）
- **删**：大型项目模板（web/ 176MB，需要时再装）
- **留**：Agent 日常工作流（plan、bug-hunter、systematic-debugging、squirrel、hermes-agent-skill-authoring）
- **留**：用户实际在用的核心技能（hermes-cortex、design、taste-critic、research、obsidian）

详见 `references/skill-audit-workflow.md`。

## 技能管理

### Skill Management Skill

完整的技能管理流程：审计、清理、优化、自进化。

关键教训：
- **不要过度删除** — plan、bug-hunter、systematic-debugging 等是 Agent 日常工作流，必须保留
- **两个目录都要清理** — `~/.hermes/skills/` 和 `D:\Hermes\skills\`
- **用户确认再删** — 永远不要自动删除，先展示分析再问用户

详见 `devops/skill-management` skill。

### Optimization Plan

5 阶段优化方案（2026-06-15 研究）：

| 阶段 | 任务 | 预期效果 | 状态 |
|------|------|----------|------|
| 1 | 上下文压缩（SOUL.md 11.7KB→<1KB） | -30K tokens/turn | ✅ 完成 |
| 2 | 技能自进化（描述优化 + DSPy + GEPA） | +30% 质量 | ✅ 完成（简化版） |
| 3 | 知识图谱升级（enhanced_graph.py） | +50% 检索准确率 | ✅ 完成 |
| 4 | 成本优化（fallback_models + compression） | -50% 成本 | ✅ 完成 |
| 5 | 可观测性（observability.py） | 全链路追踪 | ✅ 完成 |

详见 `devops/skill-management` skill 的 `references/optimization-plan.md`。

### 清理标准

**删除**：
- 用户场景不匹配（Windows 用户删 Apple/macOS、不搞音乐删 media、不用 Docker 删 s6）
- 从没用过且无明确场景的（gaming、gifs、libreoffice 等 Antigravity 装的）
- 大型项目模板（web/ 176MB，需要时再装）

**保留**：
- Agent 日常工作流（plan、bug-hunter、systematic-debugging、squirrel、hermes-agent-skill-authoring）
- 用户实际在用的核心技能（hermes-cortex、design、taste-critic、research、obsidian）

### 实际清理记录 (2026-06-14)

| 指标 | 清理前 | 清理后 | 减少 |
|------|--------|--------|------|
| 技能数 | 117 | 44 | -62% |
| 每轮消耗 | ~300,265 tokens | ~113,586 tokens | -62% |

删除的类别：Apple(5)、Gaming(2)、Smart-Home(1)、Social-Media(1)、Red-Teaming(1)、media(5)、data-science(1)、email(1)、Creative大部分(16)、MLOps大部分(7)、Productivity大部分(6)、Research部分(3)、Autonomous-AI-Agents大部分(4)、s6调试工具(3)、Root视频工具(3)、agenttrace(1)

合并：devops/data-organization + devops/hermes-data-management → hermes-data-management

**⚠️ 关键教训**：清理时不要过度删除 Agent 自身在用的技能。用户会问"你平时用不到吗？"——plan、bug-hunter、systematic-debugging、squirrel、hermes-agent-skill-authoring 等是 Agent 日常工作流的一部分，必须保留。

## 参考资源

### Skill Management
- `devops/skill-management` — 完整的技能管理流程：审计、清理、优化、自进化
  - `references/skill-audit-workflow.md` — 审计工作流
  - `references/optimization-plan.md` — 5 阶段优化方案

### SOUL.md 压缩
- `references/soul-compression.md` — SOUL.md 压缩指南（11.7KB → 777 bytes）

### 设计模式
详见 `references/automation-pattern.md` — **"Brain" 类 skill 必须是自动的**。

### Harness Engineering
- `references/harness-engineering-overview.md` — 综合研究：5 个子系统、核心项目、演进路径
- `references/harness-engineering-comprehensive.md` — 深度研究：30+ 项目、AutoHarness 6步治理
- `references/harness-engineering-deep.md` — 课程研究：12 讲、6 个项目
- `references/harness-engineering-research.md` — 基础概念
- `references/harness-engineering.md` — 基础研究
- `references/ahe-observability.md` — AHE 可观测性驱动：Sprint 合同、评估者评分表、消融实验、Anthropic 三角色架构实验数据

### Skill 审计
详见 `references/skill-audit-workflow.md` — hermes-skill-audit 工具使用方法、清理标准、实际案例（117→59 skills，300K→136K tokens）。

### GitHub
- `references/github-upload-workflow.md` — 上传工作流（解决 MCP 截断问题）
- `references/github-readme-pattern.md` — 专业 README 格式参考
- `references/github-readme-format.md` — README 结构模板
- `references/github-comparison.md` — 20+ 项目对比分析
- `references/github-projects.md` — GitHub 项目列表

### 其他
- `references/karpathy-llm-wiki.md` — Karpathy LLM Wiki 模式详解
- `references/hermes-hooks-integration.md` — Shell Hooks 集成指南
- `references/shell-hooks-integration.md` — Shell Hooks 配置详情
- `references/research-methodology.md` — 研究方法论

### AHE 可观测性增强（待实现）

基于 AHE 深度研究的改进方向（详见 `references/ahe-observability.md`）：

| 组件 | 说明 | 优先级 | 预期收益 |
|------|------|--------|----------|
| **Sprint 合同** | 任务前协商"完成"的定义，防止过早宣布完成 | 🔥 高 | +30% 效率 |
| **评估者评分表** | 量化质量评估，将"好不好"转化为可量化评分 | 🔥 高 | +40% 质量 |
| **消融实验** | 逐个移除组件，量化每个组件的贡献 | ⭐ 中 | 识别关键组件 |
| **三角色架构** | Planner + Generator + Evaluator 分离 | ⭐ 中 | 提升可控性 |
- `references/health-check-procedure.md` — 全面体检流程

### Hermes Starter
`hermes-starter` skill — Agent 自配置 Runbook，8 个配置阶段。

### 中文资源
| 资源 | 链接 |
|------|------|
| AI Agent 记忆机制综述 | https://zhuanlan.zhihu.com/p/1995813479794353043 |
| 2026年Agent记忆系统方案横评 | https://cloud.tencent.com/developer/article/2665379 |
| Karpathy LLM Wiki 原文 | https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f |

---

## Shell Hooks 集成详情

详见 `references/shell-hooks-integration.md` — 完整的配置格式、Python wrapper、allowlist、验证命令。

---

## 总结

Hermes 大脑系统是一个基于 Karpathy LLM Wiki 模式的知识管理系统，它通过：

1. **结构化存储** — 实体、概念、探索、日记四种笔记类型
2. **知识图谱** — 实体 + 关系的网络结构
3. **三层检索** — 热缓存 + 索引 + 图谱的检索架构
4. **自动维护** — 孤立检测、关联推荐、过期清理、引用验证
5. **自进化循环** — discover → suggest → search → extract → create → update

实现了知识的持续累积和智能检索，让 Hermes 真正拥有一个"大脑"。

---

## Hermes 原生融合（Shell Hooks）

Hermes 有原生 Shell Hooks 系统，可以让 cortex 真正融入 Hermes 生命周期。

**配置方式**（在 `AppData/Local/hermes/config.yaml`，不是 `~/.hermes/config.yaml`）：

```yaml
hooks:
  on_session_start:
    - command: "python D:/Hermes/skills/hermes-cortex/scripts/hook_session_start.py"
      timeout: 30
  on_session_end:
    - command: "python D:/Hermes/skills/hermes-cortex/scripts/hook_session_end.py"
      timeout: 60
hooks_auto_accept: true
```

⚠️ **`command` 必须是单个完整字符串**，不能拆成 `command` + `args`。源码用 `shlex.split()` 解析。

**可用事件**：`on_session_start`, `on_session_end`, `on_session_finalize`, `post_tool_call`, `pre_llm_call`, `post_llm_call`, `subagent_start`, `subagent_stop`

**验证**：`hermes hooks list` / `hermes hooks test <event>` / `hermes hooks doctor`

详见 `references/hermes-hooks-integration.md`。

---

## 自动化工作流

```
用户提问
    ↓
自动检索（retrieve.py）
    ↓
回答问题
    ↓
学习新知识
    ↓
自动创建笔记（evolve.py）
    ↓
更新索引（semantic_index.py）
    ↓
更新热缓存（hot_cache.py）
    ↓
等待下一次提问
```

---

## ⚠️ 关键 Pitfall（续）

### 10. "大脑"必须是全自动的

用户明确指出："这个skill不是大脑吗，还要特定使用吗"。**一个需要手动触发的"大脑"不是大脑，只是一堆脚本。**

- ❌ 提供 `python scripts/xxx.py` 命令让用户手动跑
- ❌ 问用户"要我运行这个吗"
- ✅ 配置 Shell Hooks，会话结束自动运行
- ✅ 配置 Windows 计划任务，每天自动运行自进化循环
- ✅ 所有操作后台执行，用户无感知

### 13. 更新 cron 时间要改 3 个地方

当用户要求改变 cron 时间时，需要同时更新：
1. `scripts/cron.py` — Python 脚本中的 crontab 命令
2. `scripts/cron_task.sh` — Shell 脚本中的注释
3. GitHub — 上传更新后的文件

### 14. GitHub 同步完整性检查

**不主动同步 GitHub，用户让更新再更新。** 不要每次改完文件就上传 GitHub。用户明确说："哪有天天更新的"。

上传文件到 GitHub 后，**必须验证本地和远程文件列表一致**。不能只看上传成功就完事。

```bash
# 本地文件列表
cd D:/Hermes/skills/hermes-cortex && find . -type f -name "*.py" -o -name "*.md" -o -name "*.sh" | sort

# 远程文件列表
curl -s -H "Authorization: token $TOKEN" "https://api.github.com/repos/$OWNER/$REPO/git/trees/main?recursive=1" | python -c "import sys,json; [print(x['path']) for x in json.load(sys.stdin).get('tree',[])]"
```

**对比两个列表**，找出缺失的文件再补传。常见遗漏：
- shell 脚本 (.sh) — 容易漏
- 新增的 references 文件
- 更新过的 SKILL.md（大小是否匹配）

### 15. 技能膨胀警告

当技能数量超过 50 个时，需要清理。信号：
- 同类技能 5+ 个（如设计相关 6 个）
- 来历不明的技能（如 Antigravity 装的 gaming、gifs、libreoffice）
- 磁盘占用异常（如 web/ 176MB 的项目模板）

清理时注意：**不要删除 Agent 自身在用的技能。** plan、bug-hunter、systematic-debugging、squirrel、hermes-agent-skill-authoring 等是 Agent 日常工作流的一部分。用户会问"你平时用不到吗？"——答案是"用得到"。

### 16. 研究深度要求

用户对技术介绍有两种模式：
- **正式模式**：用于研究报告、Obsidian 笔记
- **通俗模式**：用于面对面介绍，用比喻、简短句子、不要术语

当用户说"通俗一点"时，切换到通俗模式。用日常比喻（如"装系统→点外卖"），避免技术术语堆砌。

### 15. 技能膨胀警告

创建 Windows 计划任务时，用 PowerShell cmdlet 而不是 XML：

```powershell
# ✅ 正确：用 cmdlet
$action = New-ScheduledTaskAction -Execute 'path\to\script'
$trigger = New-ScheduledTaskTrigger -Daily -At '21:00'
$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries
Register-ScheduledTask -TaskName 'Task Name' -Action $action -Trigger $trigger -Settings $settings

# ❌ 错误：用 XML（编码问题导致失败）
Register-ScheduledTask -TaskName 'Task Name' -Xml 'path\to\xml'
```

**任务名称**：`Hermes Cortex Auto Evolve`（不是 hermes-brain）
**触发器**：每天 21:00 + 电脑空闲 10 分钟后
**空闲设置**：用 `$settings.IdleSettings` 配置（`-AtIdle` 参数不存在）：

```powershell
$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries
$settings.IdleSettings.IdleDuration = "PT10M"
$settings.IdleSettings.WaitTimeout = "PT1H"
$settings.IdleSettings.StopOnIdleEnd = $false
$settings.IdleSettings.RestartOnIdle = $true
```

### 16. 研究深度要求

用户说"好好研究"或"吃透"时，意思是**多来源深度研究**，不是只看一两个仓库。

**正确做法**：
1. GitHub 搜索多个关键词（harness engineering、agent harness、coding agent harness）
2. 按星数排序，读 Top 10-20 项目的 README
3. 找到权威来源（OpenAI、Anthropic、LangChain、Martin Fowler）
4. 找到中文资源（Datawhale、清华、鱼皮）
5. 找到框架实现（AutoHarness、AHE）
6. 找到书籍（harness-books、claude-code-book）
7. 综合所有来源，创建完整研究报告

**错误做法**：
- 只读一两个仓库就说"研究完成"
- 只看英文资源，忽略中文资源
- 只看理论，不看实际实现

**用户原话**："我是让你吃透harness，跑各种能搜索学习的地方去"

### 17. 技能去重检查

**问题**：技能可能在多个分类目录下存在（如 `software-development/` 和 `superpowers-zh/`），导致重复加载。

**检查方法**：
```bash
# 找出重复的技能
find D:/Hermes/skills -name "SKILL.md" | while read f; do dir=$(dirname "$f"); name=$(basename "$dir"); echo "$name"; done | sort | uniq -d

# 找出重复技能的位置
find D:/Hermes/skills -name "SKILL.md" -path "*/<skill-name>/*"
```

**清理原则**：保留英文原版（通常更完整），删除翻译版。如果内容不同（非简单翻译），保留内容更丰富的版本。

**实际案例**（2026-06-15）：
- `requesting-code-review` — software-development/（英文原版） vs superpowers-zh/（中文翻译）→ 删除中文版
- `subagent-driven-development` — 同上
- `systematic-debugging` — 同上
- `test-driven-development` — 同上

**安装新技能前**：先检查是否已存在同名技能，避免重复。

### 18. 执行前先讲清楚流程

用户说"讲清楚点，是什么流程"或"一步一步来"时，意思是**先解释整个流程是什么，再开始执行**。

**正确做法**：
1. 先用通俗语言解释整个流程（用比喻、简短句子）
2. 说明每个步骤是干嘛的
3. 说明预期效果
4. 然后问"要从哪一步开始？"或"要开始吗？"
5. 用户确认后才开始执行

**错误做法**：
- 直接开始执行，不解释流程
- 用技术术语堆砌，用户看不懂
- 一次解释太多，信息过载

**用户原话**："讲清楚点，是什么流程"、"一步一步来"

**通俗解释模板**：
```
就像给汽车做保养：
1. 瘦身 = 清理后备箱（去掉没用的东西）
2. 技能升级 = 调校发动机（让每个零件工作更好）
3. 知识升级 = 换更好的导航（找路更准）
4. 省钱 = 换更省油的驾驶方式
5. 看得见 = 装个油耗表（知道油花在哪）
```

---

### 19. 技能加载机制（不会臃肿）

用户担心"自动识别技能会导致臃肿"。实际上技能是**按需加载**的：

| 状态 | 说明 | Token 消耗 |
|------|------|-----------|
| **未加载** | 技能存在但不占用上下文 | 0 |
| **摘要加载** | 只加载名称和描述（几句话） | ~100-200 |
| **完整加载** | 读取 SKILL.md 全文 | ~1K-10K |

**触发条件**：
- 关键词匹配（"写代码" → 加载 coding 相关技能）
- 用户主动调用（"用 hermes-cortex"）
- 上下文相关（提到 GitHub → 加载 github 技能）
- 无关话题不触发

**44 个技能的实际消耗**：
```
大部分时间：只加载摘要（~100-200 tokens/技能）
需要时：才完整加载 SKILL.md
44 技能 × 摘要 ~150 tokens = ~6,600 tokens
实际每轮：~113K tokens（包含对话+工具+技能）
```

**放心**：技能按需加载，不会每次对话都塞满上下文。

---

## 日志

自动化日志保存在：`D:\ObsidianVault\.hermes_logs\`

## 记忆管理

当记忆接近 10,000 字符上限时：
1. 优先替换低优先级或临时条目
2. 使用 `memory(action='replace', old_text='...')` 更新现有条目
3. 如果找不到精确匹配，用更长的子字符串
4. 知识存储规则：详细知识存 Obsidian，记忆只放一句话索引
